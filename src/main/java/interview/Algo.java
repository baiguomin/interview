package interview;

/**
 *
 */
public class Algo {
	/**
	 * 1.如何寻找到链表的中间位置
	 * 	 第一种方法：如果是双向链表，两边迭代循环到中间
	 *	 第二种方法，两个指针，步长一个是1，一个是2
	 *	 当步长是2的指针到尾的时候，步长为1的指向的就是中间位置
	 * 2.如何判断两个商家的共同营业时间
	 * 	 将商家的共同营业时间（分钟级别）搞成1440的数组，营业的时间为1，不营业为0
	 *   然后做0/1操作，同为1的才为1
	 * 
	 * 
	 */
}
